# Changelog

이 프로젝트의 주요 변경을 기록합니다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/), 버전은 [Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다.

## [1.2.11] - 2026-08-13

### Added
- **승격 템플릿 1종** — A/B 테스트 결과 판정 메모(R34). 라이브러리 43종. `analysis` 카테고리에 실험 가설·사전 성공 기준(주 지표·임계값·유의성)·실측 결과를 넣으면, 주 지표가 기준을 못 넘으면 등급을 실패로 고정하고 사전 미등록 보조 지표의 개선을 완화 근거로 쓰지 않은 채 참고 관찰로 분리해 성공/부분 달성/실패/판정불가를 정직하게 판정하는 실험 판정형 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.32 — 완전 신설·완전 삭제 등급 고정(v1.31) 재현 확인 + 강화: 실험·A/B 결과 판정 메모 도메인에서 등급 고정 규칙 유무만 다른 통제쌍으로 재검한 결과, 재량형은 이번엔 등급 라벨은 정확했으나 그 재량이 판정 근거 서술(raw 밖 인과 해석·용도를 벗어난 제안)로 새어 들어갔다 — 등급 고정 원칙이 막는 것은 라벨 왜곡만이 아니라 판정 서술 전반의 재량 확장이라는 메커니즘을 v1.31에 통합.

## [1.2.10] - 2026-08-12

### Added
- **승격 템플릿 1종** — 규정·정책 개정 대조표(R33). 라이브러리 42종. `analysis` 카테고리에 개정 전후 원문을 조항 단위로 넣으면 각 변경을 문구적 변경/실질적 변경/위치 이동으로 등급 매기고, 실질적 변경과 등급 확인이 필요한 항목만 걸러 승인권자에게 확인 요청으로 정리하는 대조형 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.31 — 완전 신설·완전 삭제는 등급·분류를 판정 여지 없이 고정 신규: 두 시점(또는 목록↔원문)을 비교하는 대조·감사형 도메인에서, 완전히 사라진 항목이 불확정 대체 문구(예: "별도 공지")로 이어받아도 그 대체 문구의 존재가 "수정"류 중립 카테고리로 완화 분류하는 근거가 되지 않음을 통제 실측으로 확인 — raw 창작 금지·역방향 커버리지 스캔과 다른 세 번째 축(실재 사실을 중립 카테고리로 뭉뚱그려 무게를 축소하지 않는다)으로 신규 승격.

## [1.2.9] - 2026-08-11

### Added
- **승격 템플릿 1종** — 장애·이슈 원인 감별 보고서(R32). 라이브러리 41종. `analysis` 카테고리에 장애 증상과 raw 타임라인을 넣으면 시간이 겹치는 모든 원인 후보를 먼저 나열한 뒤 채택/소거/보류로 감별하고, 감별 결론과 재발방지 조치 확인 요청까지 만드는 사고 조사형 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.30 — 감별 도메인(후보 나열-판정 순서 분리) 신규: 여러 원인·가설 후보를 감별하는 도메인은 후보 나열과 채택/소거/보류 판정을 같은 지시 안에서 동시에 시키면 조기 고착이 재현된다는 것을 실측 — 나열을 판정보다 앞선 별도 필수 단계로 분리하는 요구사항을 감별형 문서의 표준 구조에 추가. 결정 요청 상단 고정(v1.4·v1.23)도 조건화 — 작성자와 최종 결정권자가 분리된 감사형 도메인에서는 상단에 판단·권고를 확정적으로 고정하는 대신 사실 집계 + 확인 필요 라우팅만 두어야 함을 통제쌍 실측으로 확정(backlog 방향 가설 채택).

## [1.2.8] - 2026-08-10

### Added
- **승격 템플릿 1종** — 규정·요건 대조표(R31). 라이브러리 40종. `analysis` 카테고리에 계약·약관·정책 조항을 요건 목록과 1:1로 대조해 충족/부분충족/미충족/확인 필요로 판정하고, 원문을 역방향으로 재스캔해 요건 목록에 없는 특이 조항까지 짚어내는 감사형 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.29 — 역방향 커버리지 스캔 신규: 고정 목록을 원문과 대조하는 감사형 도메인은 목록→원문 정방향 대조만으로는 원문에만 있는 리스크(자동 갱신, 일방적 변경 권한 등)를 구조적으로 놓친다는 것을 실측 — 원문을 역방향으로 재스캔해 목록 밖 특이 조항을 오분류 없이 별도 절로 뽑아내는 요구사항을 감사형 문서의 표준 구조에 추가.

## [1.2.7] - 2026-08-09

### Added
- **승격 템플릿 1종** — 데이터 검산 메모(R30). 라이브러리 39종. `analysis` 카테고리에 보고서·발표자료의 수치 주장을 원본 데이터와 1:1로 대조해 일치/불일치/확인불가로 판정하고, 재계산 과정은 원문과 구분해 노출하며 불확실한 항목은 담당자에게 확인 요청까지 만드는 검산형 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.28 — 자기검증 루프 내장(v1.7·v1.9·v1.26) 정제: v1.26↔v1.27의 정제 질문(루프의 위험은 유무인가, 요구하는 행위인가)을 통제쌍 실측으로 해결. 베이스와 루프 존재를 동일하게 고정하고 요구 행위만 재확인형(누락 탐지만)·생성형(원인·반론의 사후 생성)으로 가르자 생성형에서만 raw 밖 값이 일관되게 창작됨을 확인 — 자기검증 루프는 재확인형만 허용, 생성형은 금지로 정제.

## [1.2.6] - 2026-08-08

### Added
- **승격 템플릿 1종** — 회의 결정 추적 메모(R29). 라이브러리 38종. `report` 카테고리에 회의 결정을 담당자·시점·근거 발언과 함께 추적하고, 불확실한 결정은 확정/조건부확정/확인필요로 구분해 담당자에게 확인 요청까지 만드는 원장형 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.27 — 확인 상태 라벨 + 인간 라우팅 신규: 여러 독립 결정을 추적하는 문서에서 불확실한 항목을 확정된 항목과 뭉뚱그리지 않고 명시적 상태로 구분한 뒤, 답을 아는 사람에게 구체적 확인 요청으로 라우팅하는 설계를 실측(첫 검증, 재현 대기). 같은 라운드에서 자기검증 루프 원천차단 vs 사후반박 방향 가설을 통제 재검 — v1.26과 반대 방향 결과가 나와 충돌 판정으로 DNA는 건드리지 않고 정제 질문만 남김.

## [1.2.5] - 2026-08-07

### Added
- **승격 템플릿 1종** — 고객 온보딩 첫 메시지(R28). 라이브러리 37종. `email` 카테고리에 가입 직후 첫 활성화를 겨냥한 온보딩 메시지 템플릿이 처음 들어왔다(기존 이메일류는 재구매·CS·아웃리치 등 기존 관계 대상).

### Changed
- 번들 DNA v1.26 — 자기검증 루프 내장(v1.7·v1.9) 강화: 자기검증이 raw에 없는 저항 지점을 스스로 만들어 반박하는 창작이 부속 메모뿐 아니라 최종 발송 본문에까지 직접 삽입되는 유형을 확인, "필드로 주어지지 않은 저항 지점은 반박하지 않는다"는 원천 차단형 설계가 더 안전한 대안임을 실측.

## [1.2.4] - 2026-08-06

### Added
- **승격 템플릿 1종** — 긴 자료 핵심 요약(R27). 라이브러리 36종. `summary` 카테고리에 보고서·논문·긴 스레드 등 비회의형 장문 자료를 다루는 템플릿이 처음 들어왔다(기존 `meeting-summary`는 회의록 한정).

### Changed
- 번들 DNA v1.25 — raw 창작 금지 강화(계산이 정확해도 원문 문장으로 없으면 창작 — 파생 수치 하위 사례 추가) + 발송 세트 착지 v1.20 조건부 예외 강화(신규 도메인: 사내 보고서 요약. 검토용 자가점검 로그는 배제가 최선이고, 꼭 넣어야 한다면 부속 분리보다 본문 노출이 차악이라는 배치 타이브레이커 추가).

## [1.2.3] - 2026-08-05

### Added
- **승격 템플릿 1종** — 사용자 리서치 인터뷰 질문 설계(R26). 라이브러리 35종. `research` 카테고리에 실제 인터뷰에서 그대로 진행하는 구어체 질문 대본형 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.24 — 발송 세트 착지(R1) + 다음 관문 대비물(R6) 강화: 실행형 문서는 실행 직후 휘발되는 산출물(원문 발화 등)을 붙잡을 기록 틀까지 부속 세트에 포함해야 하고, 그 틀이 라이브러리 내 다운스트림 템플릿(`interview-insights`)의 입력 포맷과 맞으면 파이프라인이 이어진다.

## [1.2.2] - 2026-08-04

### Added
- **승격 템플릿 1종** — 지표 리포트 해석 메모(R25). 라이브러리 34종. `analysis` 카테고리에 판단 착지형(계속/중단/조정을 최상단에 고정) 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.23 — 결정 요청 상단 고정(R5) + 행동 착지(R11) 결합이 지표 해석·판단 도메인에서 재확인됐다.

## [1.2.1] - 2026-08-04

### Added
- **승격 템플릿 1종** — 채용 공고 작성(R24). 라이브러리 33종. `career` 카테고리에 처음으로 **고용주 관점** 템플릿이 들어왔다(기존 3종은 전부 구직자 관점).

### Changed
- 번들 DNA v1.22 — raw에 없는 세부 창작 금지 강화(채용 공고 도메인 재현).

## [1.2.0] - 2026-08-02

### Added
- **승격 템플릿 2종** — 서비스 FAQ 페이지 작성(R22), 보도자료 작성(R23). 라이브러리 30종 → **32종**.
- **사용 기록에 `layer`·`matched_slug`** — 실행이 1층(라이브러리 히트)이었는지 2층(뼈대 없이 조립)이었는지와, 1층이면 어떤 템플릿이 쓰였는지를 남긴다. 2층 기록만 모으면 라이브러리가 못 덮은 작업이 드러나고, `matched_slug`는 라이브러리 적중률을 준다. 모순된 값(1층인데 슬러그 없음 / 2층인데 있음)은 기록을 오염시키므로 던진다.

### Changed
- 번들 DNA v1.19 → **v1.21**. 조건부 예외 1건(통째로 게시되는 문서는 검토용 부속물 배제, R22)과 신규 원칙 1건(완성 예시 동봉 — 통제 재검으로 순효과 격리, R23)이 반영됐다.
- 3층 훅이 `layer: 2` 기록만 본다 — 1층은 이미 라이브러리가 덮은 작업이라 백로그 제안 대상이 아니다.

## [1.1.0] - 2026-07-31

### Added
- **승격 라이브러리 동봉** — 진화 루프가 라틴 방진 블라인드 심사로 승격한 템플릿 30종을 플러그인에 실어 나른다. 볼트가 없는 레포에서도 검증된 뼈대를 쓸 수 있다.
- **자동 3층 라우팅** — 1층: 라이브러리에 맞는 템플릿이 있으면 그 뼈대에 대화 맥락을 채운다 / 2층: 없으면 기존대로 DNA 4요소로 깎는다 / 3층: 같은 작업이 반복되면 진화 백로그 타깃 제안(훅만, 판정 임계는 미정).
- **`--evolve`** — 볼트가 있는 레포에서만 진화 라운드로 진입한다. 없으면 거부한다(공장은 원본 레포에 있다).
- **사용 기록** — 실행마다 `~/.reprompt/usage.jsonl`에 1건. 쓰기 실패가 실행을 막지 않는다.

### Changed
- 번들 DNA·라이브러리가 **빌드 산출물**이 됐다(`scripts/build-plugin-bundle.mjs`). 수동 `cp` 제거, `wiki-lint`가 드리프트를 FAIL로 차단한다 — 종전 검사는 WARN이라 DNA를 어긋내도, 라이브러리를 통째로 지워도 통과했다.
- 번들 DNA v1.14 → **v1.19**.

## [1.0.0] - 2026-07-21

### Added
- `reprompt` 메타프롬프팅 스킬 — 대화 맥락을 프롬프트 DNA(v1.14)로 맞춤 프롬프트로 깎아 `.reprompt/<날짜>-<slug>/`에 6파일(BRIEF/PROMPT/RATIONALE/OUTPUT/INSPECTION.md + meta.json)로 산출·실행·점검하는 6단계 루프.
- 실행환경 변환 4타깃: general / coding(Goal 중지요건 + Ultracode 제약) / image(구도·피사체·스타일·조명·카메라) / research(출처·범위·검증).
- Claude Code 플러그인 패키징: `plugin/` 배포 단위 + 자체 마켓플레이스(`repick-prompt`), `${CLAUDE_SKILL_DIR}` 기반 이식성, 번들 DNA.

[1.2.1]: https://github.com/SONGYEONGSIN/repick-prompt/releases/tag/reprompt--v1.2.1
[1.2.0]: https://github.com/SONGYEONGSIN/repick-prompt/releases/tag/reprompt--v1.2.0
[1.1.0]: https://github.com/SONGYEONGSIN/repick-prompt/releases/tag/reprompt--v1.1.0
[1.0.0]: https://github.com/SONGYEONGSIN/repick-prompt/releases/tag/reprompt--v1.0.0
