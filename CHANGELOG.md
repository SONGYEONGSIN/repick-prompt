# Changelog

이 프로젝트의 주요 변경을 기록합니다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/), 버전은 [Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다.

## [1.2.20] - 2026-08-24

### Added
- **승격 템플릿 1종** — 견적·청구 대조표(R43). 라이브러리 52종. `analysis` 카테고리에 견적서·청구서 항목을 넣으면, 항목별로 일치/변경(서면 근거 있음)/확인필요를 판정하고 청구서 전체를 역방향으로 다시 훑어 견적에 없던 신규 청구 항목까지 짚어내며, 서명·접수 확인이 있는 서면 근거와 구두·문자 확인만 있는 진술을 구분해 재량으로 격상시키지 않는 감사표 템플릿이 처음 들어왔다 — 감사 가능성 축이 처음으로 견적서와 청구서 두 문서를 대조한다.

### Changed
- 번들 DNA v1.41 — 집계 자기모순 방지 신규: 상태별 건수를 상단에 요약하는 감사형·대조형 문서는, 그 숫자를 본문 카드를 실제로 세어서 산출해야 한다. 라틴 방진 3라운드 전부에서, 상단 요약이 특정 라벨을 "0건"이라 명시했는데 바로 아래 본문 카드는 정확히 그 라벨을 달고 있는 자기모순을 3명의 독립 심사자 전원이 위치와 무관하게 지목했다 — 과거 두 라운드(R33·R40)에 곁가지 관찰로만 남아 있던 같은 유형의 실패가 세 번째 독립 재현되어 독립 원칙으로 신설했다.

## [1.2.19] - 2026-08-23

### Added
- **승격 템플릿 1종** — 성능 회귀 감별 보고서(R42). 라이브러리 51종. `analysis` 카테고리에 신고된 성능 저하와 raw 측정치·기준선을 넣으면, 재현·발생을 가르는 조건 가설(브라우저·데이터량·시간대·부하 등)마다 카드를 만들어 판정하고 신고 항목 커버리지 대조표로 누락 여부를 검증하며, 서로 다른 조건에서 측정된 값을 하나의 판정에 섞지 않고 조건이 다르면 정직하게 판정 불가로 남기는 감사표 템플릿이 처음 들어왔다 — 감사 가능성 축이 처음으로 신고 체감과 raw 측정치·기준선을 대조한다.

### Changed
- 번들 DNA v1.40 — raw 창작 금지(v1.9·v1.25) 강화: 개별적으로는 참인 두 사실(서로 다른 판정 카드·조건에서 확인된 사실)을 결합해 그 사이의 인과관계를 새로 서술하는 것도 단일 사실을 날조하는 것과 같은 축의 창작이다. 라틴 방진 3라운드 전부에서, "측정 조건 동등성" 컬럼을 명시 강제한 후보조차 그 컬럼이 다루지 않는 자유 서술 구간에서 raw에 없는 결합 인과("A는 B와 결합되어 나타난 결과")를 확정 서술해 3명의 독립 심사자 전원에게 동일하게 지적됐다 — 새 항목 추가 없이 기존 원칙에 이 세부 사례를 덧붙였다.

## [1.2.18] - 2026-08-22

### Added
- **승격 템플릿 1종** — 계약 이행 현황 점검표(R41). 라이브러리 50종. `analysis` 카테고리에 계약이 약속한 조건(기한·수량·금액)과 실제 이행 기록을 넣으면, 조건마다 이행 확인 근거를 "공식 문서"와 "구두·미확정 보고"로 별도 컬럼 분리해 대조하고 이행/부분이행/미이행/확인필요로 판정하며, 계약에 없지만 이행 기록에서 발견된 특이사항을 역방향으로 짚어내고 확인 요청까지 만드는 감사표 템플릿이 처음 들어왔다 — 감사 가능성 축이 처음으로 계약이 약속한 미래 조건과 실제 이행 기록을 대조한다.

### Changed
- 번들 DNA v1.39 — 확인 상태 라벨(v1.27) 강화: "확인 필요" 라벨은 원문 표현이 모호해 판단 불가한 경우뿐 아니라, 조건의 트리거 시점이 아직 도래하지 않아 판정 대상 자체가 아닌 경우도 포함한다. 라틴 방진 3라운드 전부에서, 아직 준공 전이라 시작되지 않은 하자보수 의무를 "이행 기록이 없으면 미이행"이라는 요구사항 문언을 기계적으로 적용해 오판정한 후보가 3명의 독립 심사자 전원에게 자기모순으로 지적됐다 — 새 항목 추가 없이 기존 원칙에 이 사유를 덧붙였다.

## [1.2.17] - 2026-08-21

### Added
- **승격 템플릿 1종** — 번역·표기 일관성 점검표(R40). 라이브러리 49종. `analysis` 카테고리에 원문과 번역문을 문장 단위로 넣으면 정확/의미 변경/누락/표기·용어 불일치 넷으로 판정하고, 번역문 전체를 별도로 다시 훑어 원문에 없는 첨언(승인되지 않은 내용 추가)까지 잡아내며, 판정이 애매한 항목은 게시 결정권자가 아니라 실제로 답을 아는 원 번역자에게 확인 요청으로 라우팅하는 감사표 템플릿이 처음 들어왔다 — 감사 가능성 축이 처음으로 "원문 문장 ↔ 번역 문장" 언어 축 대조를 다룬다.

### Changed
- 번들 DNA v1.38 — 역방향 커버리지 스캔(v1.29) 세 번째 도메인 재현(강화) + 확인 상태 라벨·인간 라우팅(v1.27) 재현(강화): 라틴 방진 3라운드 전부 만장일치로 역방향 스캔을 갖춘 후보가 원문에 없는 첨언을 빠짐없이 포착(대조 입도가 조항에서 문장으로 바뀌어도 유효), 확인 요청을 결정권자가 아니라 실제로 답을 아는 사람에게 보낸 후보가 3라운드 전부 무결점 — 둘 다 새 항목 추가 없이 기존 원칙에 근거 라운드만 덧붙였다.

## [1.2.16] - 2026-08-20

### Added
- **승격 템플릿 1종** — 버그 재현 절차 보고서(R39). 라이브러리 48종. `analysis` 카테고리에 신고자 원문·로그·재현 시도 기록을 넣으면 신고된 증상마다 근거 강도(인용 가능한가)와 출처 정체성(신고자 발화/로그·계측/재현자 직접 확인)을 별도 컬럼으로 판정해 재현됨/재현 안 됨/판정 불가로 정직하게 가르고, "데이터가 통째로 날아간다"·"거의 매번" 같은 신고자의 과장된 주장이 근거 없이 사실로 격상되지 않게 막으며, 판정 불가 항목은 실제 답을 아는 담당자에게 확인 요청으로 라우팅하는 재현 검증 템플릿이 처음 들어왔다 — 감사 가능성 축이 처음으로 신고자 발화/로그/재현자 직접 확인 3원 출처 구조를 다룬다.

### Changed
- 번들 DNA v1.37 — 출처 강도와 출처 정체성 분리 컬럼(v1.36) 두 번째 도메인 재현(강화·조건부): 베이스를 완전히 동일하게 고정하고 검증 카드의 판정 방식만 분리형(별도 두 컬럼) vs 합산형(신뢰도 단일 등급)으로 가른 통제쌍에서, 분리형이 라틴 방진 3라운드 전부 환각·과장억제 축에서 합산형을 앞섰다(29 vs 27/30) — 방향은 재확인. 다만 R38의 핵심 오분류 시그니처(2차 정보의 최상위 등급 승격)는 이번엔 관찰되지 않았고 격차도 R38(+12)보다 얇았으며(+2) 산출물 헤더 누락 confound가 있어, 새 원칙을 추가하지 않고 v1.36에 두 번째 도메인 근거만 조건부로 덧붙였다.

## [1.2.15] - 2026-08-19

### Added
- **승격 템플릿 1종** — 요구사항 출처 역추적 표(R38). 라이브러리 47종. `analysis` 카테고리에 이미 작성된 산출물의 항목과 원본 자료를 넣으면, 항목마다 원문을 직접 인용하며 근거를 직접 근거/부분 근거·간접 근거/출처 없음으로 판정하고, 근거 강도와 출처 유형(고객 직접 발화/2차 보고·내부 판단/데이터 분석)을 분리 표기해 2차 보고를 고객 직접 요청으로 부풀리지 않으며, 확인이 필요한 항목은 승인권자가 아니라 실제 근거를 아는 사람에게 라우팅하는 감사표 템플릿이 처음 들어왔다 — 감사 가능성 축이 처음으로 "산출물 항목 → 근거" 역방향 대조를 다룬다.

### Changed
- 번들 DNA v1.36 — 출처 강도와 출처 정체성 분리 컬럼 신규: "인용 가능한 근거가 있는가"와 "그 근거가 누구의 말인가(고객 직접 발화/2차 보고·내부 판단/데이터 분석)"는 서로 다른 두 축이며, 하나의 등급 라벨로 합치면 2차 보고가 1차 근거로 오분류 승격되는 오류가 구조적으로 생긴다 — 라틴 방진 3라운드 블라인드 심사에서 이 분리가 없는 후보만 3라운드 전부 이 오류로 환각·과장억제 최하위(16/30)를 받고, 분리가 있는 두 후보는 완전 동률 최고점(28/30)을 받았다. "감의 출처는 감으로 남긴다"(v1.14, 서술 단계 금지)를 템플릿 구조 설계 원칙으로 보강.

## [1.2.14] - 2026-08-18

### Added
- **승격 템플릿 1종** — 벤더·업체 비교 평가표(R37). 라이브러리 46종. `analysis` 카테고리에 여러 벤더의 제안서와 비교 기준을 넣으면 기준마다 벤더 전원을 원문 인용과 함께 4단계로 판정하고, 근거가 아예 없는 조합은 확인 필요로 고정하며, 우선순위를 주지 않으면 근거 없는 종합 점수·순위를 만들지 않는 벤더 비교표 템플릿이 처음 들어왔다 — 감사 가능성 축이 처음으로 N개 대상 × 동일 기준 구조를 다룬다.

### Changed
- 번들 DNA v1.35 — anti-slop 구조 마커(①②③) 금지 강화: 어느 후보 템플릿도 지시하지 않았는데 다중 기준×대상 비교표를 생성시킨 독립 실행 2/3회가 동그라미 숫자 구조 마커를 자발적으로 신설해 3명의 심사자 전원이 3라운드 모두 감점 — 비교·대조형(N×M 그리드) 도메인은 이 금지를 요구사항 문장으로 명시할 것.

## [1.2.13] - 2026-08-17

### Added
- **승격 템플릿 1종** — 문의 이관 인계 메모(R36). 라이브러리 45종. `report` 카테고리에 고객 문의 대화 이력을 넣으면, 우리 쪽이 이미 한 약속(조치·기한)을 원문 인용·시점·기한·처리 상태 4요소로 추적하고 인계 시점과 대조해 기한 경과 여부를 명시적으로 계산하며, 나머지 정보는 사실/추정/미확인으로 갈라 다음 담당자가 바로 이어받아 응대할 수 있게 정리하는 인계형 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.34 — 약속 이행 추적 신규: 인계·감사형 문서에서 제3자에게 이미 표명된 약속은 확인 상태 라벨(v1.27) 3분류와 별개의 전용 구조로 승격시켜, 작성 시점과 대조한 이행 상태(완료/진행중/미착수/확인 필요, 기한 경과 여부)를 명시적으로 계산한다 — 라틴 방진 3라운드 전부 만장일치로 확인.

## [1.2.12] - 2026-08-14

### Added
- **승격 템플릿 1종** — 지원자 평가 기록 정리(R35). 라이브러리 44종. `career` 카테고리에 평가 기준 목록과 면접 발언 원문을 넣으면 기준마다 원문 인용과 함께 4단계(충족/부분충족/미충족/판단보류)로 판정하고, 근거 발언이 없는 기준은 재량으로 승격시키지 않고 판단보류로 고정하며, 면접관의 비공식 인상에 위법·차별 소지가 섞여도 판정 근거로 옮기지 않고 원문을 역방향으로 재훑어 목록 밖 특이 발언까지 결정권자에게 확인 요청으로 넘기는 고용주 관점 평가 기록 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.33 — 위법·차별 소지 인상 필터링 신규: 판정·심사형 문서에서 raw 메모에 섞인 위법·차별 소지 인상(나이·혼인 등)은 판정 근거로 쓰지 않고 원문에서 정당한 근거를 재탐색, 없으면 판단보류로 고정 — R24(채용 공고, 작성형)의 필터링 기법을 판정형 문서 일반으로 확장. 완전 신설·완전 삭제 등급 고정(v1.31·v1.32)도 다섯 번째 도메인에서 방향을 재확인(강화·조건부 — 이번엔 메커니즘이 서술 재량 확장이 아니라 형식 완결성 격차로 나타남).

## [1.2.11] - 2026-08-13

### Added
- **승격 템플릿 1종** — A/B 테스트 결과 판정 메모(R34). 라이브러리 43종. `analysis` 카테고리에 실험 가설·사전 성공 기준(주 지표·임계값·유의성)·실측 결과를 넣으면, 주 지표가 기준을 못 넘으면 등급을 실패로 고정하고 사전 미등록 보조 지표의 개선을 완화 근거로 쓰지 않은 채 참고 관찰로 분리해 성공/부분 달성/실패/판정불가를 정직하게 판정하는 실험 판정형 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.32 — 완전 신설·완전 삭제 등급 고정(v1.31) 재현 확인 + 강화: 실험·A/B 결과 판정 메모 도메인에서 등급 고정 규칙 유무만 다른 통제쌍으로 재검한 결과, 재량형은 이번엔 등급 라벨은 정확했으나 그 재량이 판정 근거 서술(raw 밖 인과 해석·용도를 벗어난 제안)로 새어 들어갔다 — 등급 고정 원칙이 막는 것은 라벨 왜곡만이 아니라 판정 서술 전반의 재량 확장이라는 메커니즘을 v1.31에 통합.

## [1.2.10] - 2026-08-12

### Added
- **승격 템플릿 1종** — 규정·정책 개정 대조표(R33). 라이브러리 42종. `analysis` 카테고리에 개정 전후 원문을 조항 단위로 넣으면 각 변경을 문구적 변경/실질적 변경/위치 이동으로 등급 매기고, 실질적 변경과 등급 확인이 필요한 항목만 걸러 승인권자에게 확인 요청으로 정리하는 대조형 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.31 — 완전 신설·완전 삭제는 등급·분류를 판정 여지 없이 고정 신규: 두 시점(또는 목록↔원문)을 비교하는 대조·감사형 도메인에서, 완전히 사라진 항목이 불확정 대체 문구(예: "별도 공지")로 이어받아도 그 대체 문구의 존재가 "수정"류 중립 카테고리로 완화 분류하는 근거가 되지 않음을 통제 실측으로 확인 — raw 창작 금지·역방향 커버리지 스캔과 다른 세 번째 축(실재 사실을 중립 카테고리로 뭉뚱그려 무게를 축소하지 않는다)으로 신규 승격.

## [1.2.9] - 2026-08-11

### Added
- **승격 템플릿 1종** — 장애·이슈 원인 감별 보고서(R32). 라이브러리 41종. `analysis` 카테고리에 장애 증상과 raw 타임라인을 넣으면 시간이 겹치는 모든 원인 후보를 먼저 나열한 뒤 채택/소거/보류로 감별하고, 감별 결론과 재발방지 조치 확인 요청까지 만드는 사고 조사형 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.30 — 감별 도메인(후보 나열-판정 순서 분리) 신규: 여러 원인·가설 후보를 감별하는 도메인은 후보 나열과 채택/소거/보류 판정을 같은 지시 안에서 동시에 시키면 조기 고착이 재현된다는 것을 실측 — 나열을 판정보다 앞선 별도 필수 단계로 분리하는 요구사항을 감별형 문서의 표준 구조에 추가. 결정 요청 상단 고정(v1.4·v1.23)도 조건화 — 작성자와 최종 결정권자가 분리된 감사형 도메인에서는 상단에 판단·권고를 확정적으로 고정하는 대신 사실 집계 + 확인 필요 라우팅만 두어야 함을 통제쌍 실측으로 확정(backlog 방향 가설 채택).

## [1.2.8] - 2026-08-10

### Added
- **승격 템플릿 1종** — 규정·요건 대조표(R31). 라이브러리 40종. `analysis` 카테고리에 계약·약관·정책 조항을 요건 목록과 1:1로 대조해 충족/부분충족/미충족/확인 필요로 판정하고, 원문을 역방향으로 재스캔해 요건 목록에 없는 특이 조항까지 짚어내는 감사형 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.29 — 역방향 커버리지 스캔 신규: 고정 목록을 원문과 대조하는 감사형 도메인은 목록→원문 정방향 대조만으로는 원문에만 있는 리스크(자동 갱신, 일방적 변경 권한 등)를 구조적으로 놓친다는 것을 실측 — 원문을 역방향으로 재스캔해 목록 밖 특이 조항을 오분류 없이 별도 절로 뽑아내는 요구사항을 감사형 문서의 표준 구조에 추가.

## [1.2.7] - 2026-08-09

### Added
- **승격 템플릿 1종** — 데이터 검산 메모(R30). 라이브러리 39종. `analysis` 카테고리에 보고서·발표자료의 수치 주장을 원본 데이터와 1:1로 대조해 일치/불일치/확인불가로 판정하고, 재계산 과정은 원문과 구분해 노출하며 불확실한 항목은 담당자에게 확인 요청까지 만드는 검산형 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.28 — 자기검증 루프 내장(v1.7·v1.9·v1.26) 정제: v1.26↔v1.27의 정제 질문(루프의 위험은 유무인가, 요구하는 행위인가)을 통제쌍 실측으로 해결. 베이스와 루프 존재를 동일하게 고정하고 요구 행위만 재확인형(누락 탐지만)·생성형(원인·반론의 사후 생성)으로 가르자 생성형에서만 raw 밖 값이 일관되게 창작됨을 확인 — 자기검증 루프는 재확인형만 허용, 생성형은 금지로 정제.

## [1.2.6] - 2026-08-08

### Added
- **승격 템플릿 1종** — 회의 결정 추적 메모(R29). 라이브러리 38종. `report` 카테고리에 회의 결정을 담당자·시점·근거 발언과 함께 추적하고, 불확실한 결정은 확정/조건부확정/확인필요로 구분해 담당자에게 확인 요청까지 만드는 원장형 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.27 — 확인 상태 라벨 + 인간 라우팅 신규: 여러 독립 결정을 추적하는 문서에서 불확실한 항목을 확정된 항목과 뭉뚱그리지 않고 명시적 상태로 구분한 뒤, 답을 아는 사람에게 구체적 확인 요청으로 라우팅하는 설계를 실측(첫 검증, 재현 대기). 같은 라운드에서 자기검증 루프 원천차단 vs 사후반박 방향 가설을 통제 재검 — v1.26과 반대 방향 결과가 나와 충돌 판정으로 DNA는 건드리지 않고 정제 질문만 남김.

## [1.2.5] - 2026-08-07

### Added
- **승격 템플릿 1종** — 고객 온보딩 첫 메시지(R28). 라이브러리 37종. `email` 카테고리에 가입 직후 첫 활성화를 겨냥한 온보딩 메시지 템플릿이 처음 들어왔다(기존 이메일류는 재구매·CS·아웃리치 등 기존 관계 대상).

### Changed
- 번들 DNA v1.26 — 자기검증 루프 내장(v1.7·v1.9) 강화: 자기검증이 raw에 없는 저항 지점을 스스로 만들어 반박하는 창작이 부속 메모뿐 아니라 최종 발송 본문에까지 직접 삽입되는 유형을 확인, "필드로 주어지지 않은 저항 지점은 반박하지 않는다"는 원천 차단형 설계가 더 안전한 대안임을 실측.

## [1.2.4] - 2026-08-06

### Added
- **승격 템플릿 1종** — 긴 자료 핵심 요약(R27). 라이브러리 36종. `summary` 카테고리에 보고서·논문·긴 스레드 등 비회의형 장문 자료를 다루는 템플릿이 처음 들어왔다(기존 `meeting-summary`는 회의록 한정).

### Changed
- 번들 DNA v1.25 — raw 창작 금지 강화(계산이 정확해도 원문 문장으로 없으면 창작 — 파생 수치 하위 사례 추가) + 발송 세트 착지 v1.20 조건부 예외 강화(신규 도메인: 사내 보고서 요약. 검토용 자가점검 로그는 배제가 최선이고, 꼭 넣어야 한다면 부속 분리보다 본문 노출이 차악이라는 배치 타이브레이커 추가).

## [1.2.3] - 2026-08-05

### Added
- **승격 템플릿 1종** — 사용자 리서치 인터뷰 질문 설계(R26). 라이브러리 35종. `research` 카테고리에 실제 인터뷰에서 그대로 진행하는 구어체 질문 대본형 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.24 — 발송 세트 착지(R1) + 다음 관문 대비물(R6) 강화: 실행형 문서는 실행 직후 휘발되는 산출물(원문 발화 등)을 붙잡을 기록 틀까지 부속 세트에 포함해야 하고, 그 틀이 라이브러리 내 다운스트림 템플릿(`interview-insights`)의 입력 포맷과 맞으면 파이프라인이 이어진다.

## [1.2.2] - 2026-08-04

### Added
- **승격 템플릿 1종** — 지표 리포트 해석 메모(R25). 라이브러리 34종. `analysis` 카테고리에 판단 착지형(계속/중단/조정을 최상단에 고정) 템플릿이 처음 들어왔다.

### Changed
- 번들 DNA v1.23 — 결정 요청 상단 고정(R5) + 행동 착지(R11) 결합이 지표 해석·판단 도메인에서 재확인됐다.

## [1.2.1] - 2026-08-04

### Added
- **승격 템플릿 1종** — 채용 공고 작성(R24). 라이브러리 33종. `career` 카테고리에 처음으로 **고용주 관점** 템플릿이 들어왔다(기존 3종은 전부 구직자 관점).

### Changed
- 번들 DNA v1.22 — raw에 없는 세부 창작 금지 강화(채용 공고 도메인 재현).

## [1.2.0] - 2026-08-02

### Added
- **승격 템플릿 2종** — 서비스 FAQ 페이지 작성(R22), 보도자료 작성(R23). 라이브러리 30종 → **32종**.
- **사용 기록에 `layer`·`matched_slug`** — 실행이 1층(라이브러리 히트)이었는지 2층(뼈대 없이 조립)이었는지와, 1층이면 어떤 템플릿이 쓰였는지를 남긴다. 2층 기록만 모으면 라이브러리가 못 덮은 작업이 드러나고, `matched_slug`는 라이브러리 적중률을 준다. 모순된 값(1층인데 슬러그 없음 / 2층인데 있음)은 기록을 오염시키므로 던진다.

### Changed
- 번들 DNA v1.19 → **v1.21**. 조건부 예외 1건(통째로 게시되는 문서는 검토용 부속물 배제, R22)과 신규 원칙 1건(완성 예시 동봉 — 통제 재검으로 순효과 격리, R23)이 반영됐다.
- 3층 훅이 `layer: 2` 기록만 본다 — 1층은 이미 라이브러리가 덮은 작업이라 백로그 제안 대상이 아니다.

## [1.1.0] - 2026-07-31

### Added
- **승격 라이브러리 동봉** — 진화 루프가 라틴 방진 블라인드 심사로 승격한 템플릿 30종을 플러그인에 실어 나른다. 볼트가 없는 레포에서도 검증된 뼈대를 쓸 수 있다.
- **자동 3층 라우팅** — 1층: 라이브러리에 맞는 템플릿이 있으면 그 뼈대에 대화 맥락을 채운다 / 2층: 없으면 기존대로 DNA 4요소로 깎는다 / 3층: 같은 작업이 반복되면 진화 백로그 타깃 제안(훅만, 판정 임계는 미정).
- **`--evolve`** — 볼트가 있는 레포에서만 진화 라운드로 진입한다. 없으면 거부한다(공장은 원본 레포에 있다).
- **사용 기록** — 실행마다 `~/.reprompt/usage.jsonl`에 1건. 쓰기 실패가 실행을 막지 않는다.

### Changed
- 번들 DNA·라이브러리가 **빌드 산출물**이 됐다(`scripts/build-plugin-bundle.mjs`). 수동 `cp` 제거, `wiki-lint`가 드리프트를 FAIL로 차단한다 — 종전 검사는 WARN이라 DNA를 어긋내도, 라이브러리를 통째로 지워도 통과했다.
- 번들 DNA v1.14 → **v1.19**.

## [1.0.0] - 2026-07-21

### Added
- `reprompt` 메타프롬프팅 스킬 — 대화 맥락을 프롬프트 DNA(v1.14)로 맞춤 프롬프트로 깎아 `.reprompt/<날짜>-<slug>/`에 6파일(BRIEF/PROMPT/RATIONALE/OUTPUT/INSPECTION.md + meta.json)로 산출·실행·점검하는 6단계 루프.
- 실행환경 변환 4타깃: general / coding(Goal 중지요건 + Ultracode 제약) / image(구도·피사체·스타일·조명·카메라) / research(출처·범위·검증).
- Claude Code 플러그인 패키징: `plugin/` 배포 단위 + 자체 마켓플레이스(`repick-prompt`), `${CLAUDE_SKILL_DIR}` 기반 이식성, 번들 DNA.

[1.2.1]: https://github.com/SONGYEONGSIN/repick-prompt/releases/tag/reprompt--v1.2.1
[1.2.0]: https://github.com/SONGYEONGSIN/repick-prompt/releases/tag/reprompt--v1.2.0
[1.1.0]: https://github.com/SONGYEONGSIN/repick-prompt/releases/tag/reprompt--v1.1.0
[1.0.0]: https://github.com/SONGYEONGSIN/repick-prompt/releases/tag/reprompt--v1.0.0
